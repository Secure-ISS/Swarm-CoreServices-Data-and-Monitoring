global:
  resolve_timeout: 5m

route:
  receiver: 'default'
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  group_by:
    - cluster
    - alertname
    - severity

receivers:
  - name: 'default'
    # Console logging only - no external integrations needed for dev
